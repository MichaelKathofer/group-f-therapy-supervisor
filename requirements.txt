# =============================================================================
# Supervisor - Requirements
# =============================================================================
# Core dependencies for running the supervisor model
# Install with: pip install -r requirements.txt
# =============================================================================

# PyTorch (CUDA required for GPU inference)
torch>=2.0.0

# Transformers & Model Loading
transformers>=4.36.0
accelerate>=0.25.0
peft>=0.7.0

# Quantization (4-bit inference)
bitsandbytes>=0.41.0

# Data Processing
datasets>=2.14.0
pandas>=2.0.0

# Utilities
tqdm>=4.66.0
safetensors>=0.4.0

# =============================================================================
# Optional: Only needed for retraining (not inference)
# =============================================================================
# trl>=0.8.0          # For SFTTrainer
# unsloth             # For faster training (pip install unsloth)
# ollama              # For label generation with teacher model
